apiVersion: v1
kind: Service
metadata:
  name: medicine-service-svc
spec:
  selector:
    app: medicine-service
  ports:
    - name: medicine-service-port
      protocol: TCP
      port: 8082
      targetPort: 8082
  type: ClusterIP
