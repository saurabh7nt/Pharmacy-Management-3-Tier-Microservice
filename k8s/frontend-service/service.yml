apiVersion: v1
kind: Service
metadata:
  name: frontend-service-svc
spec:
  selector:
    app: frontend-service
  ports:
    - name: frontend-service-port
      protocol: TCP
      port: 5714
      targetPort: 80
  type: ClusterIP
